{
  "name": "denta.dexa.be",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "installDep": "concurrently \"npm install\" \"npm install --prefix user-service\"",
    "migrate_db":"npx prisma migrate dev --name init",
    "generate_db":"npx prisma generate"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "@types/node": "^24.2.0",
    "typescript": "^5.9.2"
  },
  "dependencies": {
    "@prisma/client": "^6.13.0",
    "concurrently": "^9.2.0",
    "prisma": "^6.13.0",
    "rimraf": "^6.0.1"
  }
}
